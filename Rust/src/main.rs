use std::{collections::HashMap, i32};

pub struct Solution;
const MOD: i32 = 109 + 7;

impl Solution {
    pub fn shift_distance(
        s: String,
        t: String,
        next_cost: Vec<i32>,
        previous_cost: Vec<i32>,
    ) -> i64 {
      
    }
}

fn main() {}
